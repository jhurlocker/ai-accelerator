apiVersion: v1
kind: ResourceQuota
metadata:
  name: tenant-quota-cpu
  namespace: jh-project-shared
spec:
  hard:
    limits.cpu: '8'
    limits.memory: 16Gi
    limits.nvidia.com/gpu: '0'
    pods: '20'
    requests.cpu: '4'
    requests.memory: 8Gi
    requests.nvidia.com/gpu: '0'
